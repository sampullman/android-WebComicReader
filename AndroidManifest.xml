<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.threeDBJ.comicReader"
          android:versionCode="21"
          android:versionName="4"
          android:installLocation="preferExternal">
  <uses-sdk android:minSdkVersion="5"
            android:targetSdkVersion="13"/>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <application android:label="@string/app_name"
               android:icon="@drawable/ic_main"
	           android:name="ComicReaderApp">
    <activity android:name=".comicReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".reader.Reader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.SMBCReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.XKCDReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.FeelAfraidReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.ExplosmReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.PHDReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.ButtersafeReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.QuestionableContentReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.DinosaurReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.CtrlAltDeleteReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.PennyArcadeReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".SavedComicMenu"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".SavedComicReader"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.AbstruseGooseReader"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.ManlyGuysReader"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.CompletelySeriousReader"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity android:name=".reader.MongrelDesignsReader"
              android:theme="@android:style/Theme.NoTitleBar">
    </activity>
    <activity
	  android:name="com.google.ads.AdActivity"
	  android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
  </application>
</manifest>
